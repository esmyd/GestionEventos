# Configuración para servir el frontend React (SPA)
# El frontend hace peticiones directamente a https://gestioneventos.siglotecnologico.com/api

# Habilitar rewrite engine
RewriteEngine On

# Para archivos estáticos (assets, imágenes, etc.), servirlos normalmente
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Para todas las demás rutas, servir index.html (SPA routing)
# Esto permite que React Router maneje las rutas del frontend
RewriteRule . /index.html [L]
