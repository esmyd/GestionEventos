# Configuración para servir el frontend React (SPA)
# El frontend hace peticiones directamente a https://gestioneventos.siglotecnologico.com/api
# El dominio ge.siglotecnologico.com apunta a esta carpeta (/frontend)

# Habilitar rewrite engine
RewriteEngine On

# Si la petición es para un archivo o directorio que existe, servirlo directamente
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Redirigir todo lo demás a dist/ (donde está el build de producción)
# Si el build está directamente en frontend/, cambiar dist/ por ./
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ dist/$1 [L]

# Si la petición es la raíz, servir index.html del build
RewriteCond %{REQUEST_URI} ^/$
RewriteRule ^$ dist/index.html [L]
