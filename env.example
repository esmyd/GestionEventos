# ===========================================
# CONFIGURACIÓN DE LIRIOS EVENTOS
# ===========================================
# Copia este archivo como .env y configura los valores
# NO subas el archivo .env a git (ya está en .gitignore)

# Ambiente: development | production
ENVIRONMENT=development

# ===========================================
# BASE DE DATOS
# ===========================================
DB_HOST=localhost
DB_USER=tu_usuario
DB_PASSWORD=tu_contraseña
DB_NAME=lirios_eventos
DB_PORT=3306

# ===========================================
# SEGURIDAD JWT
# ===========================================
# IMPORTANTE: En producción, genera una clave segura y única
# Puedes usar: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=genera-una-clave-segura-de-al-menos-32-caracteres
JWT_EXPIRATION_HOURS=24

# ===========================================
# SERVIDOR API
# ===========================================
API_HOST=0.0.0.0
API_PORT=5000
DEBUG_MODE=false

# URL del frontend (para CORS en producción)
FRONTEND_URL=https://tu-dominio.com

# ===========================================
# WHATSAPP CLOUD API
# ===========================================
WHATSAPP_TOKEN=tu_token_de_whatsapp
WHATSAPP_PHONE_NUMBER_ID=tu_phone_number_id
WHATSAPP_WEBHOOK_TOKEN=tu_webhook_verify_token

# ===========================================
# EMAIL SMTP (opcional, puede estar en BD)
# ===========================================
# SMTP_HOST=mail.tudominio.com
# SMTP_PORT=465
# SMTP_USER=notificaciones@tudominio.com
# SMTP_PASSWORD=tu_contraseña_email
